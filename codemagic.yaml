workflows:
  ios-workflow:
    name: iOS Build
    environment:
      xcode: latest
    scripts:
      - name: Build ipa
        script: |
          xcode-project build-ipa \
            --project "hotfiregame.xcodeproj" \
            --scheme "hotfiregame"
    artifacts:
      - build/ios/ipa/*.ipa
